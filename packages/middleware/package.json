{
  "name": "@agent-passport/middleware",
  "version": "0.0.1",
  "description": "Express, Fastify, and Next.js middleware for agent-passport",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "files": ["dist"],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/priyansh-x/agent-passport",
    "directory": "packages/middleware"
  },
  "dependencies": {
    "@agent-passport/core": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
