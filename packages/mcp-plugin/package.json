{
  "name": "@passport-agent/mcp",
  "version": "0.1.0",
  "description": "MCP middleware for Agent Passport — protect tool calls with passport authorization",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "keywords": ["agent", "passport", "mcp", "middleware", "ai", "tool-guard"],
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/priyansh-x/agent-passport",
    "directory": "packages/mcp-plugin"
  },
  "dependencies": {
    "@passport-agent/core": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
